# MySQL
spring.datasource.url=${dbname}
spring.datasource.username=${dbuser}
spring.datasource.password=${dbpass}
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

# S�lectionner un "dialecte" permet � Hibernate de g�n�rer du SQL adapt� � la version choisie
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect

# permet d'utiliser une configuration du server avec un fichier .env
spring.config.import=optional:file:.env.properties

# Autoriser l'affichage des requ�tes SQL faites par Hibernate
spring.jpa.show-sql=true

# G�rer Hibernate
spring.jpa.hibernate.ddl-auto=update
# 